import MapSvg from "@/assets/icons/map.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const MapIcon = (props: SvgIconProps) => <SvgIcon svg={MapSvg} {...props} />;

export default MapIcon;
