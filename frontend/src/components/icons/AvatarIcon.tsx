import AvatarSvg from "@/assets/icons/avatar.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const AvatarIcon = (props: SvgIconProps) => <SvgIcon svg={AvatarSvg} {...props} />;

export default AvatarIcon;
