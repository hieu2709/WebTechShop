import BackTopSvg from "@/assets/icons/back_top.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const BackTopIcon = (props: SvgIconProps) => <SvgIcon svg={BackTopSvg} {...props} />;

export default BackTopIcon;
