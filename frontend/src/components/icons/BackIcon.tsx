import BackSvg from "@/assets/icons/back.svg?component";
import SvgIcon, { SvgIconProps } from "@/components/SvgIcon";

const BackIcon = (props: SvgIconProps) => <SvgIcon svg={BackSvg} {...props} />;

export default BackIcon;
